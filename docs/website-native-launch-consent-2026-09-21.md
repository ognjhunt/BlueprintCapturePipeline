# Website consent at native launch

ADP-009D / day-21. Website evaluation activation reached the native launch preparer, which rejected the original `website_scene_preparation.v1` and `website_native_rights_admission.v1` records because it expected legacy source and rights schemas.

The native preparer now reads the original records without rewriting them. It verifies transport hashes, embedded seals, scene, owner, task, consent and execution authority together. Only exact derived assets from the configured revision are eligible for provider staging. This does not admit raw capture video, training, redistribution or physical-proof claims.

The retained request for evaluation `team-eval-5aea93d1-ab8a-4fe5-995a-48a0a22f90bb` passes candidate-code context validation as service account `blueprint`. Evidence is under `/var/lib/blueprint/task-evaluation-inputs/stage-replays/activation-validation-q4xljtsv/`. Its CPU bundle was created in `cpu-bundle-7uvggbpz/`; the scratch authority also validates. An isolated manifest publication performed full-byte object-store readback. None of these artifacts is a production completion receipt, profile registration or GPU allocation.

The scratch bundle retains the saved request's `8953e24f` implementation identity. Its unchanged allocator must therefore be replayed from that release, not a newer candidate checkout; the candidate identity is correctly refused. The consent adapter itself is validated from candidate `5e25b87ca`.

Completion still requires the controller's actual launch preparation receipt and native execution evidence. The captured room's placement integration remains pending; this run exercises the explicitly approved authored development surface.

The matching-release allocator dry run passed. Profile construction then exposed a second deterministic boundary: concatenating website scene/task IDs, release SHA and revision exceeds the dispatcher's 192-character profile-ID limit. The shared builder now preserves short IDs and gives long valid IDs a readable prefix plus SHA-256 of the entire original identity. Distinct revisions remain distinct; original source, scene and task fields remain intact. This was found before deploying the consent fix.
