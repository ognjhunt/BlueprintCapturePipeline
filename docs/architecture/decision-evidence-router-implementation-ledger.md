# Decision/Evidence Router implementation ledger

Status values are `verified`, `implemented`, `incomplete`, `contradicted`, and
`intentionally_deferred`. This is a living ledger for the controlled migration;
it is not completion evidence by itself.

## Baseline

| Item | Status | Evidence |
| --- | --- | --- |
| Starting source | verified | Dedicated linked worktree and branch `codex/decision-evidence-router-20260729` created from `origin/main` at `ac8148a1429ca3f704bfdec2c26cc6f2d1cdb49c`. |
| Upstream synchronization | verified | Worktree fast-forwarded without conflict to `b98a2a11` after `origin/main` advanced; no user-owned primary-checkout state was touched. |
| User-owned dirty work | verified | Primary checkout was 17 commits behind with a modified `docs/CHANGELOG.md`; it was not stashed, reset, copied, or edited. |
| Nested instructions | verified | No nested `AGENTS.md` files exist below the repository root. |
| Required doctrine read | verified | Root `AGENTS.md`, `PLATFORM_CONTEXT.md`, `WORLD_MODEL_STRATEGY_CONTEXT.md`, `VISION.md`, `docs/DOCTRINE_PRECEDENCE.md`, `README.md`, `pyproject.toml`, `docs/architecture/ai-onboarding-map.md`, and `AUTONOMOUS_ORG.md` read at the starting SHA. |
| Focused baseline | verified | Initial host lane reported 72 passed and one environment-only import failure (`defusedxml` absent). `python -m pip install -e '.[dev]'` installed the declared environment; affected Python 3.12 lanes now pass. |

## Architectural inventory

| Item | Status | Current evidence and migration decision |
| --- | --- | --- |
| Canonical leaf execution contract | verified | `src/blueprint_pipeline/evaluation_run_contract.py` defines `evaluation_run.v1` with the six required replaceable components and is consumed by `evaluation_run_execution.py`, robot-eval, and G1 kitchen adapters. It remains the stable leaf boundary. |
| Duplicate `EvaluationRunSpec` name | implemented | Static pack type renamed `LegacyEvaluationPackSpec`; the old alias remains, and `legacy_evaluation_pack_to_leaf_spec` performs tested explicit translation to canonical `evaluation_run.v1`. |
| Leaf execution | verified | `evaluation_run_execution.py` resolves exactly one preselected execution adapter and binds output to the leaf spec digest. The router must sit above this module and may compile zero, one, or many leaves. |
| Existing reusable proof machinery | verified | `robot_eval_calibration.py`, `buyer_claim_ceiling.py`, `proof_contracts.py`, `policy_ranking_thesis.py`, Task/Scenario/Eval cards, provider envelopes, and WAM/classical-sim disagreement artifacts are present. They remain lower-level evidence inputs and compatibility surfaces. |
| Current product doctrine | implemented | Canonical living docs, README, scenario suite, CLI help, and default robot-eval/Arena orchestration now expose Task Evaluation Run as the product. Legacy builders are deprecated and require explicit evidence-export opt-in. |
| Claim-level router | implemented | Versioned request/testbed/method/qualification/plan/result/decision/outcome contracts, deterministic router, conditional escalation, stable adapters, aggregation, and learning loop are implemented. |
| Current policy-ranking verdict | verified | The frozen repo result remains `thesis_not_supported`; router work must preserve that exact three-verdict vocabulary and cannot qualify its own evaluator. |

## Finite terminal gates

| Gate | Status | Required proof |
| --- | --- | --- |
| One-product doctrine and default surfaces | implemented | Canonical living docs, CLI help, manifests, and scenario suites expose Task Evaluation Run as the sole product; legacy machinery is internal/deprecated and absent by default. |
| Legacy compatibility | implemented | Tested translators cover EvaluationRunSpec pack definitions, legacy data-export requests, Policy Improvement inputs, and WAM/classical-sim artifacts without granting qualification. |
| Versioned router contracts | implemented | Maintained testbed, request, method, qualification, plan, normalized result, decision, and physical-outcome contracts have deterministic serialization plus checked JSON Schemas. |
| Deterministic routing | implemented | Tests cover exact qualification/scope, rights/privacy/provider restrictions, inputs, availability, reproducibility, budget/latency, false-safe/coverage, dominance, correlation, conditional escalation, and abstention. |
| Stable method adapters | implemented | All seven required families are representable; analytic/capture and accepted physical evidence are non-leaf, physical execution is forbidden, and availability is not qualification. |
| Leaf compilation/execution | implemented | Router emits zero/one/many canonical leaves, preserves non-leaf methods, and normalized results bind request/plan/profile/qualification/leaf/testbed digests. |
| Vertical slice | implemented | Checked rigid-object fixture proves multi-method planning, two leaves plus analytic/capture steps, unqualified world-model rejection, ranking/deployment abstention, disagreement, partial decision, ceilings, and later immutable learning. |
| Learning loop | implemented | Exact prediction/outcome join is append-only; duplicate and calibration/held-out leakage fail closed; new testbed/qualification versions keep transfer disabled. |
| WebApp handoff | verified | Versioned schemas, state machine, translations, redaction, compatibility policy, checksum manifest, and decision/abstention/partial fixtures pass `python -m blueprint_pipeline.decision_evidence_handoff`. |
| Focused verification | verified | 160 router/contract/legacy tests and 33 WAM/default-orchestration tests pass; Ruff, source governance, WebApp handoff verification, and `git diff --check` pass. |
| Fast lane | verified | The single local bare lane ran all 5,597 selected tests: 5,596 passed and the quality-gap ledger alone detected changed source digests. The repository rebind tool refreshed 13 digest bindings and the exact failing governance test then passed. On router implementation candidate `3a77f3ab`, hosted run `30478926371` then selected 5,599 tests and passed 5,591 with 8 skipped and 1,664 deselected. The local bare lane was not repeated. |
| Verification/publication | verified | Router implementation run `30478926884` executed `scripts/pytest_full.sh`: 7,263 passed in 954.94 seconds and the fail-closed CPU evidence builder reported `status=passed`. All 16 hosted checks passed; protected-main PR #243 merged as `98484a591ec5c29e957248b65390b7c62a662b1a`. After fetch, the primary checkout proved `HEAD == main == origin/main`, matching tree `bf034335032af9f7400308b6c7a4ecb3c493fe96`, and `0 0` divergence while preserving the user-owned `docs/CHANGELOG.md` edit. |
| Live/provider/physical execution | intentionally_deferred | Explicitly unauthorized and unnecessary for the hermetic control-plane slice. This does not block sim-only functionality and cannot upgrade physical/deployment/safety claims. |

## Closeout

The control-plane migration was published through protected-main PR #243. The
exact tested candidate was `3a77f3ab14504d112603b816c20560ab12d5b6b2`; the
squash merge is `98484a591ec5c29e957248b65390b7c62a662b1a`. No paid
compute, provider execution, production deployment, live robot operation, or
physical-success claim was performed. Those remain evidence-dependent claim
upgrade lanes rather than repo-local implementation gaps.

## 2026-08-01 task/site measurement-routing extension

| Gate | Status | Evidence boundary |
| --- | --- | --- |
| Six measurement contracts | implemented | Additive requirement, site-evidence, complete capability, scoped qualification, routing-decision, and abstention contracts plus checked JSON Schema. |
| Dynamic task/site compilation | implemented | Controlled task and claim taxonomies derive capabilities and site measurements from the exact claim/testbed; explicit agent-prepared requirements remain non-authoritative. |
| Hard filtering | implemented | Version, digest, approval signature status, expiration, exact scope/range, site evidence, rights, retention, provider-training use, portability, latency, cost, and claim-ceiling gates fail closed. |
| Composite routing | implemented | Only eligible qualified capability subsets participate; their union must cover the request, and ranking is lexicographic by physical accuracy, uncertainty, scope, harmful false negatives, reproducibility, privacy, latency, and cost. |
| Agent integration | implemented | Existing Task Evaluation Supervisor and OpenAI Agents SDK method-router specialist receive the embedded deterministic measurement decision and may explain/propose around it, but cannot select, qualify, execute, or mutate proof. |
| Production method admission | intentionally_deferred | No simulator/provider/reconstruction method is qualified by the research report or this code. Admission requires the frozen R0-R8 benchmark/governance process and independent physical evidence. |
| Physical/deployment/safety proof | intentionally_deferred | A routing plan establishes none of these claims. The current policy-ranking verdict remains `thesis_not_supported`. |

## 2026-08-02 measurement-routing completion slice

| Gate | Status | Evidence boundary |
| --- | --- | --- |
| Controlled task/material taxonomies | implemented | 21 controlled task classes and 15 material regimes; generic values such as `deformable` are refused; contact-regime, deformation-family, and sensor-modality maps plus solver-alternative groups (e.g. SPH/CFD/MPM) compile a deterministic capability floor that agent-prepared requirements may extend but never lower. |
| Family claim-ceiling caps and world-model roles | implemented | Exact kinematics tops out at C2; every simulation, rendering, reconstruction, world-model, provider, and framework family at C4; only physical evidence carries C6-C8. Appearance, renderer, world-model, capture, and framework families can never hold contact, articulation, or material-dynamics qualification, and world-model roles exclude collision/force/safety/physical-success authority. |
| Capture-evidence auditor | implemented | `audit_site_evidence_profile` reports missing scale, registration, collider, articulation, material, and sensor-calibration gaps with report-accurate smallest next actions; abstentions embed the audit, and C6-C8 claims always resolve to `physical_execution`. |
| R0-R8 admission hardening | implemented | Chain-integrity with retained per-stage evidence; the R6 outcome gates R7/R8 and production eligibility; development/qualification split-leakage and held-out split-binding guards; vendor self-grading prohibitions; human-only approvals; requalification triggers suspend eligibility on engine/driver/adapter/license/pipeline/regression/monitoring changes. |
| Research intake catalog | implemented | The full 2026-08-01 landscape as validated non-production dossiers with families, Q-protocols, post-qualification ceilings, and access/rights gates; top-10 priority investigations; three qualification-benchmark blueprints; twenty standing abstentions; a `BQ` label without a signed qualification record is rejected. |
| Proposal-only agent roles | implemented | The claim interpreter proposes per-claim measurement requirements and the smallest clarification; the capture supervisor surfaces audit gaps; the scenario proposer drafts frozen R4 preregistrations it cannot approve; the deterministic kernel remains the only authorizer. |
| Worked-example suite | implemented | Hermetic tests pin reachability-to-exact-geometry, appearance-only drawer abstention, transparent-object direct observation, uncharacterized-granular abstention, cloth-checkbox and cable-solver guardrails, world-model C4 bounds, physical-test routing, and the mandatory safety abstention. |

## 2026-08-02 near/long-term automation slice

| Gate | Status | Evidence boundary |
| --- | --- | --- |
| R1 engine capability profiles | implemented | Source-verified `method_capability_profile` drafts for MuJoCo 3.11.0, Drake 1.55.0, Isaac Sim 6.0.1 (PhysX and the RTX/OpenUSD sensor path as separate methods), Newton 1.4.0, SAPIEN 3.0.3, and Project Chrono 10.0.0. Every `True` boolean is traceable to a live-fetch or report-VF manifest entry; unverified fields fail closed to `False`; determinism is `unverified` and rejected by the replay allowlist; commit/container pins are explicit `unpinned:` sentinels for R3/R7. The Chrono release-date source discrepancy is recorded, not silently resolved. Zero qualifications attached — the router abstains on all of them by construction. |
| Research/release monitoring automation | implemented | `measurement_research_monitoring` plus `scripts/measurement_research_monitor.py` diff release observations against the intake catalog and engine profiles, emitting version-change alerts, stale-profile flags, R0 intake drafts, regression-check requirements, and requalification-trigger proposals. The read-only monthly `.github/workflows/measurement-research-monitor.yml` lane checks five official public GitHub release feeds, runs bounded regressions, and retains its report for 90 days. Proposals only: neither the report nor workflow can approve, advance a stage, create catalog entries, apply a trigger, or start paid/provider/physical execution. |
| Task-level site-evidence gate | implemented | Required site evidence not implied by any solver capability (real demonstrations for long-horizon claims, dynamic-object tracks for HRI) now blocks the route directly with the evidence-mapped smallest action. |
| Worked examples | implemented | All sixteen research routing examples are now pinned one-to-one, including controller ranking, nominal-CAD insertion abstention, the photoreal-kitchen composite, partial-warehouse recapture, food-specimen identification, locomotion friction versus geometric planning, and the composite-ceiling minimum rule. |
| Engine qualification | intentionally_deferred | Verified feature lists are capability declarations, never task-scoped validity: routes still require signed, admission-bound qualification records produced by the physical benchmark blueprints. |

## 2026-08-02 adapter, benchmark, and source-monitor slice

| Gate | Status | Evidence boundary |
| --- | --- | --- |
| Adapter runtime contracts | implemented | `measurement_adapter_runtime` provides checked descriptors, non-invasive environment probes, complete tri-state capability drafts, and R0/R1 admission packets for priority geometry, rigid/contact, sensor, deformable, tactile, observation, world-model, and provider candidates. Probes do not import or launch engines, inspect credentials, authorize execution, or qualify a method. Project Chrono intentionally has no PyPI distribution probe: official PyChrono is a projectchrono conda package or source build, so an unrelated package sharing the `pychrono` name cannot create a false availability result. |
| Qualification benchmark harnesses | implemented | `measurement_qualification_benchmarks` materializes the Geometry-and-Contact, Observation, and Deformation programs, including cloth/cable/granular lanes, disjoint development/qualification split digests, sealed independent physical labels, receipt-bound predictions, deterministic metrics, and R4/R5 stage-data builders. R5 reports cannot assert R6/R7, production, physical success, deployment, or safety. |
| Task/site observation qualification matrix | implemented | `measurement_sensor_stream_pairing` binds accepted calibrated RGB/depth/LiDAR/optional-event samples and timing. `measurement_observation_qualification` then requires every opaque-control/transparent/reflective/dark/small/thin/occluded by controlled/natural/adverse-lighting pair for the exact task, site, pairing, synthetic methods, and policy checkpoint cohort, with physical/synthetic modalities and repeated paired policy outcomes. The generic observation R5 builder now fails closed unless this matrix and every required held-out metric are joined; the result is still only an R5 evidence candidate. |
| DLO-Lab source feasibility and paid CUDA canary | failed runtime observed; debugger propagation fix encoded | Official source commit `c5026a9416b03c6bc5186eba13cd4ffd4c0e7796`, declared `genesis-world` 1.0.0 metadata, GPU ROD implementation/examples, and Apache-2.0 license-file hashes are retained in `docs/evidence/measurement_dlo_lab_source_observation_2026-08-02.json`. An immutable two-case development bundle and exact-source CUDA worker run only through the canonical grant-gated Vast allocator with signed transport, an independent watchdog, retry cap zero, bounded spend/TTL, exact teardown, and provider-zero verification. Attempts 1 and 2 localized repeatable signal-6 native assertion/termination aborts to Genesis import. Attempt 3 showed isolated Torch import passed while Torch followed by Quadrants aborted on CUDA 13.0. Attempt 4 changed to the CUDA 12.8 line exercised by Quadrants' exact release workflow; with Python 3.11.14, direct Quadrants import itself aborted in both cases. Attempt 5 held source, CUDA 12.8, Torch 2.9.1+cu128, and Quadrants 0.8.0 fixed while moving to DLO-Lab's README-prescribed Python 3.12.11; direct Quadrants import still aborted in both cases before CUDA execution. Attempt 6 installed the bounded GDB diagnostic but exposed a fail-closed propagation bug: the generic adapter environment stripped the bootstrap-only flag before the case worker, so no backtrace was captured and no compatibility conclusion was added. All six failures, spend, teardown, provider-zero, watchdog, and object-store-zero records are retained in `docs/evidence/measurement_dlo_lab_vast_attempt{1,2,3,4,5,6}_2026-08-02.json`; the source-backed runtime comparison is in `docs/evidence/measurement_dlo_lab_runtime_compatibility_observation_2026-08-02.json`. CUDA and Python version changes are not sufficient fixes. The diagnostic mode is now digest-bound inside each execution request instead of expanding the generic safe environment; signed transport URLs remain stripped. The next bounded step reruns the same sanitized native debugger probe with all compatibility variables fixed. CUDA replay, physical material accuracy, Q-DLO, and R5-R7 remain unproven. |
| Primary-source snapshot monitor | implemented | `measurement_research_monitor` validates sanitized metadata from injected source fetchers, stores no raw source prose or credentials, builds immutable snapshots, diffs source/version/access changes, recommends benchmarks, emits bounded regression plans, and defines a monthly due-state contract. It is distinct from the lightweight release watcher and remains proposal-only. |
| Agent research visibility | implemented | The deterministic method-router and scenario proposer receive validated adapter descriptors, benchmark specifications, and monitor reports. Protected fields force admission, qualification, catalog mutation, execution authorization, and R7 eligibility false; research artifacts can create review proposals only. |
| Production execution and independent evidence | intentionally_deferred | Descriptors, probes, harnesses, and monitors do not prove production execution. Narrow local Pinocchio/Coal, MuJoCo, Newton, SAPIEN, PyElastica, OpenCV observation, direct tactile, and world-model development workers are recorded below; production wrappers, held-out physical datasets/results, independent R6 decisions, R7 admission, and physical/deployment/safety evidence still require separately governed systems and accountable human actions. |

## 2026-08-02 executable adapter slice

| Gate | Status | Evidence boundary |
| --- | --- | --- |
| Uniform local adapter executor | implemented | `measurement_adapter_execution` supplies exact-bound request, worker-result, receipt, and bundle contracts; argv-only/no-shell execution; reduced environment; temporary work root; explicit execute gate; timeout; bounded result; log digests without content; and strict request/result/receipt/prediction joins. Local execution rejects qualification splits and cannot authorize provider spend, physical work, production routing, R6, or R7. |
| Pinocchio/Coal exact-geometry development worker and corpus | implemented | The explicit `exact-geometry-development` environment pins Pinocchio 4.1.0 and Coal 3.0.3. `measurement_pinocchio_coal_kinematic_adapter` verifies a synthetic planar two-link analytic IK solution with Pinocchio forward kinematics, evaluates link-obstacle signed distance using Coal GJK at 101 finite joint-interpolation samples, repeats the complete trace, and refuses continuous-collision authority. `measurement_geometry_kinematic_development_suite` covers reachable-clear, reachable-discrete-collision, and unreachable boundaries. It includes no captured mesh, robot/site registration, self-collision program, continuous collision, physical labels, or R5-R7 qualification. |
| Qualified-geometry to site-evidence bridge | implemented | `measurement_site_evidence_bridge` reuses the existing observed-surface compiler and independent bounded-navigation collider qualifier, adds `measurement_robot_site_registration.v1`, validates a signed independent SE(3) robot-base-to-site-frame measurement with frozen error thresholds and task-region coverage, then emits `site_evidence_profile.v1`. Metric, candidate, qualification, registration, asset, task-region, and source-capture lineage must join exactly. Development registrations cannot validate the registration evidence, protected evidence records cannot be overridden, and the bridge is structurally incapable of creating method qualification, R5, R6, R7, physical-success, deployment, or safety authority. |
| MuJoCo rigid-contact worker and development corpus | implemented | Locked MuJoCo advanced from 3.10.0 to the report target 3.11.0. `measurement_mujoco_adapter` verifies its own implementation digest and exact engine version, runs the bounded `mujoco_rigid_drop.v1` case twice, and emits a prediction only when trace digests match. `measurement_geometry_contact_development_suite` now plans or executes checked sphere and box regimes and aggregates ground-contact count, first-contact time, and penetration under schemas that force synthetic/non-instrumented/non-R5/R6/R7 truth. This is actual local development execution, not captured-site contact accuracy or a production qualification. |
| Newton rigid-contact worker and cross-engine corpus | implemented | Newton 1.4.0 and Warp 1.15.0 are exact-pinned in the geometry and development environments. `measurement_newton_rigid_adapter` executes the shared method-neutral sphere/box corpus on CPU XPBD in `RUN_TO_RUN` mode and verifies package, implementation, backend, precision, solver, and replay identities. `measurement_geometry_contact_cross_engine_development_suite` pairs each case with MuJoCo, retaining engine-specific case/receipt digests and explicit contact-time/penetration deltas. Agreement or disagreement is development evidence only and cannot create R5-R7 or physical authority. |
| Drake rigid-contact worker and corpus | implemented | `measurement_drake_rigid_adapter` executes the shared sphere/box corpus with exact Drake 1.55.0, MultibodyPlant CPU, SAP discrete contact, and point contact. Because current Drake does not support the repository's macOS Python 3.12 runtime, `bootstrap_measurement_drake_development.py` creates an explicit isolated Python 3.13 environment and the uniform executor preserves its interpreter symlink instead of resolving away the environment. Worker and wrapper source bytes, engine, backend, solver, precision, cases, logs, receipts, and exact double replay are bound. The descriptor no longer requires the retired `drake-visualizer`. This remains synthetic development evidence without R5-R7, physical, renderer, deployment, or safety authority. |
| Isaac Sim PhysX rigid-contact worker and guarded Vast lane | implemented | `measurement_isaac_physx_rigid_adapter` and its checked suite bind exact Isaac Sim 6.0.1, a fresh metric Z-up USD stage, CPU PhysX TGS/SAP, enhanced determinism, live rigid-body positions, PhysX contact reports, penetration scope, and exact double replay without renderer or RTX sensor claims. `measurement_isaac_runtime_release`, the clean-commit deterministic input bundle, and `measurement_isaac_vast_canary` route the exact official NGC digest through the canonical Vast-first allocator with explicit spend/TTL, zero retries, independent watchdog, source/input/runtime bindings, bounded output validation, teardown, and scoped plus global provider-zero. The external run is not marked complete until a real returned receipt validates; even a completed run is synthetic development evidence, not R5-R7, captured-site accuracy, physical success, policy ranking, deployment, or safety authority. |
| PyChrono NSC spherical-granular worker and exact environment | implemented | `bootstrap_measurement_chrono_development.py` creates a new isolated Python 3.12 conda environment from the exact Project Chrono release label, rejects an existing/broad target, reads the authoritative PyChrono 10.0.0 build/channel/subdir from `conda-meta`, preloads only an environment-owned OpenMP library when present, constructs `ChSystemSMC`, and emits a digest-bound schema-checked non-authorizing receipt. `measurement_chrono_granular_adapter` uses the stable core `ChSystemNSC` CPU/Bullet/PSOR path for a separate two-regime 27-sphere corpus, binds the conda identity and replay, and reports spread, settling, contact scope, force, and penetration. The initial SMC penalty-contact experiment was rejected after unbounded spread; it was not silently tuned into evidence. This port explicitly did not use Chrono::Granular GPU and is not characterized-material DEM, pouring/tool interaction, physical evidence, or R5-R7 qualification. |
| SAPIEN PhysX rigid-contact worker and corpus | implemented | The explicit `sapien-development` environment exact-pins SAPIEN 3.0.3 while retaining the repository's headless OpenCV distribution. `measurement_sapien_rigid_adapter` executes both cases in the shared method-neutral sphere/box corpus with PhysX CPU, TGS, enhanced determinism, zero CPU workers, no renderer, and no ManiSkill runtime. `measurement_geometry_contact_sapien_development_suite` provides schema-checked plan/execute aggregation with engine-specific case and receipt identities. It verifies exact package, implementation, backend, precision, solver, and replay identities. This is a physics-only development port, not a SAPIEN/ManiSkill task benchmark, sensor/rendering claim, R5-R7 qualification, or physical authority. |
| MuJoCo articulated-joint worker and development corpus | implemented | `measurement_mujoco_articulation_adapter` has an implementation identity distinct from rigid drop and executes `mujoco_articulated_joint_travel.v1` twice for public synthetic hinge-door and slide-drawer cases. It binds mass, damping, effort, joint limits, target travel, exact MuJoCo/solver settings, and replay digests. The aggregate suite reports travel error and limit state while forcing captured-joint, instrumented-force, physical, R5, R6, and R7 fields false. It does not validate a real door/drawer or policy interaction. |
| MuJoCo peg-insertion worker and development corpus | implemented | `measurement_mujoco_insertion_adapter` separately executes `mujoco_square_peg_insertion_boundary.v1` for centered-clearance and lateral-interference public geometries. Exact double-run traces distinguish successful insertion from side-contact blockage and report signed clearance plus penetration. The checked suite forces captured geometry, instrumented force, physical labels, independent execution, R5, R6, and R7 false; it is not a robot-controlled or instrumented insertion qualification. |
| OpenCV calibrated-observation worker | implemented | `measurement_opencv_observation_adapter` executes the bounded `opencv_calibrated_observation.v1` protocol twice, verifies exact OpenCV/source/runtime settings, rejects noncanonical intrinsics, out-of-frame correspondences, degenerate 3D targets, unit/frame drift, and version drift, and measures reprojection RMSE, missing-depth fraction, depth residual, and timestamp alignment. `measurement_observation_development_suite` plans or runs both checked cases and emits a digest-bound aggregate. The corpus and suite schemas force synthetic/development truth with no physical labels, R5, R6, or R7 authority. |
| PyElastica cable-deformation worker | implemented | PyElastica 0.3.3.post2 is exact-pinned in the geometry and development environments. `measurement_pyelastica_cable_adapter` runs the bounded `pyelastica_cantilever_cable.v1` Cosserat-rod case twice and binds material, geometry, load, timestep, integrator, source, and runtime identities. `measurement_deformation_cable_development_suite` runs two regimes and aggregates displacement, segment-strain, and force results. This is synthetic Q-DLO development regression evidence, not characterized cable physics or qualification. |
| MuJoCo flex-cloth worker | implemented | `measurement_mujoco_flex_cloth_adapter` uses an implementation identity distinct from rigid MuJoCo and runs `mujoco_flex_cloth_sag.v1` twice against exact MuJoCo 3.11.0. The worker binds a four-corner-pinned 2D grid, `elastic2d=stretch`, material, gravity, ground contact, solver, timestep, warning counters, sag, edge strain, and penetration. `measurement_deformation_cloth_development_suite` runs sag and contact regimes. This does not qualify bending, self-contact, garments, topology change, or captured cloth materials. |
| MuJoCo spherical-granular worker | implemented | `measurement_mujoco_granular_adapter` runs the separately identified `mujoco_spherical_particle_column_collapse.v1` protocol twice over two synthetic regimes. It binds monodisperse sphere geometry, density, friction, contact damping, free-joint damping, staggered initial packing, solver, timestep, contact force, penetration, spread, and settling. The corpus and suite force physical characterization false: this rigid-sphere development reference is not Chrono DEM, a cohesive/nonspherical model, a pouring/tool-interaction benchmark, or granular qualification. |
| Direct tactile-sequence worker | implemented | `measurement_direct_tactile_adapter` reduces synchronized optical-marker displacement, contact intensity, and normal/shear-force sequences twice under `direct_tactile_sequence_reduction.v1`. The two-case suite observes stable contact and incipient slip, but its schema forces synthetic origin, identity-only calibration, and no physical measurements, real-sensor calibration, qualification, or force authority. TacSL/DiffTactile comparison and real sensor Q-TACT remain external evidence work. |
| World-model action-fidelity worker | implemented | `measurement_world_model_action_fidelity_adapter` reuses the strict numeric WAM action-recovery and cross-step replay contracts through the uniform executor. Its two-case synthetic suite covers within/outside-threshold outcomes while leaving action-motion correlation and policy-ranking regret unmeasured, scoring zero policy rankings, denying physics/physical-success authority, and preserving the frozen `thesis_not_supported` verdict. It evaluates supplied development checks; it does not run or qualify OSCAR, Cosmos, RoboWorld, IWS, or GigaWorld. |
| R5 benchmark evidence boundary | implemented | Predictions now require the complete validated execution receipt rather than an arbitrary digest. R5 candidacy requires qualification-split receipts proving independent executor, clean environment, immutable runtime identity, evaluator binding, and at least two repeated trials; the report computes deterministic 95% confidence intervals. |
| Supervisor receipt integration | implemented | Validated adapter bundles are visible as non-authorizing review evidence. Failed/blocked/plan-only runs can generate a human-review proposal, while agent retry, execution, qualification, catalog mutation, and production eligibility remain protected false. |
| Independent physical qualification | intentionally_deferred | No physical dataset, independent qualification runner receipt, human R6 decision, or R7 admission was fabricated. The local Pinocchio/Coal planar kinematic, MuJoCo rigid-drop/articulation/insertion/flex-cloth/spherical-granular, Newton rigid-drop, SAPIEN PhysX rigid-drop, OpenCV observation, PyElastica cable, synthetic direct-tactile, and world-model consistency results are development-only and cannot satisfy the external evidence gate. |
