# Website object/component test on an authored surface

ADP-040/050, day 28. Owner-authorized continuation of website steps 10, 12, 13
and 14 while step 11 (captured-scene registration/contact) remains pending.
The observed blocker is `support_surface_not_found_under_subject` in the retained
walkthrough. This mode does not repair or relax reconstruction geometry checks.

The existing website controller prepares a separate authored surface, preserves
the source object dimensions and observed frame/depth/mask references, translates
its candidate into an explicit development placement, and submits the existing
six-stage construction recipe and normal native controls/evaluation path. CAD
still runs through the pinned Astra CAD/Blender skill runtime. Static and native
qualification, rights, provider admission, spend, teardown and publication checks
remain required. A failed grasp is evidence, not permission to shrink the object.

Enable only explicitly approved task-context digests using the JSON-array setting
`BLUEPRINT_WEBSITE_DEVELOPMENT_TEST_TASK_DIGESTS` on Pipeline and WebApp. Empty
is the default; a reconstruction failure alone never enables this mode. Both
sides enforce the exact task binding. The native submission keeps the original
submission id and sponsorship cap, preventing a second simultaneous native budget
for this capture. Switching a submitted development test to a real-scene run
requires a separately authorized lifecycle rather than silently replacing the
immutable request.

The original preparation and failure stay under `website_scene_preparation/`.
The derived inputs live under its `development_test/` child. The scene identity
ends in `-development`, configured output in `-development-configured`; task and
receipts say authored-surface development test, with captured-scene evaluation
explicitly false. Offerings remain private and cannot acquire public-display
authority. The website labels both testbeds and result pages accordingly.

Completion artifacts: controller-origin CAD/static/native receipts, bounded
physics and controls, policy observation media, teardown/provider-zero receipts,
and signed website results for this development identity. These prove component
execution only. They do not close step 11 or establish an end-to-end evaluation
of the captured room. The existing 9/14 live-path count remains unchanged until
additional receipts justify a more specific update.

Verification so far: network-disabled service-account replay of the exact retained
14-view MapAnything result and task masks prepared the synthetic surface and all
six stage configurations in `/var/tmp/website-development-replay-zyh6vy91`.
The source preparation remained byte-identical; a second replay was idempotent.
No providers were called and no native execution is claimed by that replay.
