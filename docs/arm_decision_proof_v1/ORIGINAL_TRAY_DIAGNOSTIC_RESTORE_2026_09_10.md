# Restore the original Scene 841757 tray diagnostic

ADP-009, day-21 development-only fixed-arm rehearsal: after publishing the
completed green-marker diagnostic, the owner requested the original book-to-tray
task with controls still omitted. The completion artifact is a new immutable
packet retaining the exact tray, book, and room bytes and original non-control
scoring criteria, followed by native visibility, paired policy episode evidence,
Website delivery, and billing/teardown/provider-zero receipts. None of these
establishes a qualified comparison or physical transfer.

The original task contract is
`sha256:44ce7f523863465212d332587b900271fafcf727e708cf8fc48b38e0e1cbf561`.
Its tray is `document-tray/v2`, with asset SHA-256
`b077169b1f9583780bd15f7d8a3c3be2effd9afbe5af62819927a598dc322821`.
The original task author provenance and all manipulation criteria remain intact;
only the explicitly authorized control clauses are omitted. Current robot reset
and camera calibration are rebound to the new diagnostic contract. Static camera
framing does not establish native rendering or occlusion qualification.

For a physical destination asset, the existing pre-policy camera gate now also
requires native `task_support` semantic pixels in the external policy camera and
overview. It uses each camera's existing minimum-pixel threshold and retains the
counts in the gate receipt. A visible book alone cannot satisfy tray visibility.
The wrist camera remains subject-focused at reset; no control rollout is added.

During this preparation, the existing direct-policy adapter was observed to
update the nested success contract while leaving its adjacent digest field bound
to the old controls-required contract. The smallest fix synchronizes that digest
with the resealed contract. A regression covers both a tray destination and a
marker destination, original-request immutability, exact asset preservation, and
all non-control criteria. Existing output bytes are not rewritten.

Validation: the five marked-area adapter tests protect the omission boundary,
tray placement, and consistent contract identity. The mandatory policy lifecycle
and provider import-closure suites protect per-cell orchestration and sealed
provider imports before a paid attempt. The inherited native storage precision
fix remains separately documented in `NATIVE_SCENARIO_STORAGE_PRECISION_2026_09_10.md`.
