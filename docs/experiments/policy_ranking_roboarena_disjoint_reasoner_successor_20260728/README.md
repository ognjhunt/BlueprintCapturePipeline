# RoboArena disjoint Reasoner successor (2026-07-28)

This namespace prospectively governs the successor campaign after the terminal
`inconclusive` full-stack calibration. It does not rewrite that history.

The first paid action is limited to one already-exposed, post-unseal diagnostic
pair. Its only purpose is to prove that the pinned Cosmos3-Nano Reasoner runtime
can load and return one exact structured result on a compatible GPU. It cannot
provide ranking, Phase-B admission, WAM qualification, or independent-confirmation
credit.

In parallel, `disjoint_session_candidate_split_v1.json` freezes a label-sealed
candidate set selected from session and policy directory names only. It remains
a same-snapshot candidate until media, action-contract, endpoint, label, and
power admission checks pass. If only replayed actions are technically possible,
the result is open-loop replay qualification and cannot be promoted to live
closed-loop Phase B.

The first reusable technical preflight retained all rows and found that four
whole selected sessions lacked the required right-camera view: 91/119 rows
passed and 28/119 were retained as blocked. Before opening task metadata or
outcomes, `disjoint_session_candidate_split_amendment_v2.json` prospectively
replaced only those four sessions using a deterministic filename-completeness
rule. The amended split passed 119/119 restricted-NPZ and three-view checks.
`disjoint_technical_preflight_summary_v1.json` records both runs and the external
evidence hashes. This is technical admission evidence only, not ranking, WAM,
or live closed-loop evidence.

No provider call, GPU allocation, outcome-label read, or data upload is authorized
by these files alone. Paid execution must pass the canonical allocator, exact-source
binding, single-use external authorization, watchdog, budget, teardown, and
provider-zero gates.

## Structured-output canary result

The exact-main canary completed on source commit
`a67f6077e648d13b1b3781d6df02c5739dee8138`. The pinned Reasoner loaded on one
CUDA-compatible H100 NVL and returned one exact-schema JSON response. Estimated
GPU cost was `$0.24818`; authenticated provider inventory returned to zero.

This is a transport/runtime success and not evaluator qualification. The response
assigned zero progress to both episodes and described no task action, but also
set both stable-success fields to true with confidence 1 and uncertainty 0.
`reasoner_schema_canary_result_v1.json` therefore records a semantic-consistency
failure and awards no judging, ranking, Phase-B, WAM, or transfer credit.

`reasoner_semantic_validation_amendment_v2.json` prospectively requires every
future Reasoner result to pass cross-field stable-success validation. Invalid
responses remain in evidence and are rejected rather than silently repaired.

## Phase-B fallback prepared

`phase_b_open_loop_replay_protocol_v1.json` freezes the strongest technically
valid fallback before any Phase-B prediction or label access. The public
snapshot has recorded real policy traces but no runnable seven-policy endpoints,
so this is fallback level 3: open-loop replay qualification, not live
policy-requery closed loop and not a new independent snapshot.

The label-blind replay canary uses a real recorded trace, a valid identity-rot6d
no-motion control with an observation-bound gripper hold, shuffled, reversed,
shifted, and a real trace from another candidate policy. The native-Cosmos paid
bundle contains the existing frozen 5-condition by 2-seed causal screen plus two
structural canaries. Its immutable bundle SHA-256 is
`08a64c2a1829aee0a91a5bd787530eeff2dd7b00205ab308649ce08735a8c53e`.

OSCAR skeleton-only, OSCAR WAM, and the registered Cosmos plus OSCAR-skeleton
hybrid are blocked for these sessions because the snapshot lacks the calibrated
camera intrinsics and extrinsics required to project the Franka skeleton. An
uncalibrated overlay is not substituted. Native Cosmos is therefore the only
new-data WAM arm admitted to the causal canary. Full episodes and Gemini judging
remain contingent on that canary passing; a causal failure requires abstention
and prevents the expensive matrix.

`phase_b_native_cosmos_canary_preparation_v1.json` seals the bundle and two
read-only Vast preflights. The first correctly retained blocked attempt exposed
a shared TensorRT compute-cap ceiling being applied to this non-TensorRT Cosmos
lane. The code now explicitly admits Blackwell for this profile and a regression
test pins that contract. The second preflight verified authenticated zero
inventory and one compatible advisory offer; it did not reserve or launch it.

## High-motion native-Cosmos result

The amended high-motion canary completed on source commit `c04ce132` using one
RTX Pro 6000 WS. Both structural requests returned byte-identical direct and
Blueprint-wrapper videos, and all 12 frozen condition/seed requests completed.
Estimated GPU cost was `$0.088401` for `317.302998` live seconds. Authenticated
post-run inventory found zero task instances and zero total instances.

The scientific screen failed. Only 1/10 active same-seed comparisons visibly
separated from the no-motion future at the frozen threshold, only 2/10 active
rows rejected their strongest temporal placebo, and 0/5 active conditions were
robust across both seeds. The separate tier-1 gate found five hard-failure
windows and a session-median timing correlation of `0.02194`, below the frozen
`0.15` threshold. Blueprint therefore abstains from native Cosmos and does not
admit the full-episode matrix or evaluator spend.

This is a one-session, 17-frame open-loop causal diagnostic. It did not re-query
a policy and is underpowered for WAM qualification. The matching physical video
was retained outside provider and evaluator inputs and is published only as a
post-generation review reference. The selected session's metadata was later
opened while checking for camera calibration; the resulting one-session label
unseal is recorded explicitly, and the remaining 16 selected sessions remain
sealed. `phase_b_high_motion_native_cosmos_result_v1.json`, the cost/provider-zero
receipt, and the gallery manifest preserve the complete claim boundary.

## Interim closeout and positive-control amendment

`phase_b_terminal_admission_v1.json` records why 17 frames can resolve only the
short-horizon causal prerequisite and why that failed result did not admit a
full Phase-B matrix. A subsequent independent first-principles audit identified
a narrower missing gate: the exact deployment had never been tested against a
known-good published forward-dynamics pathway. The prospective
`phase_b_positive_control_bisection_amendment_v1.json` therefore supersedes only
the future-execution recommendation, not the completed result.

The amended run uses NVIDIA's hash-pinned four-chunk AgiBotWorld example first.
At least three chunks must pass frozen visible-motion thresholds. Failure stops
the run with zero DROID requests and forces teardown; a pass admits only the
already-frozen 12-request DROID diagnostic in the same loaded process. Neither
outcome by itself earns DROID WAM qualification, ranking, or independent
confirmation credit. `terminal_verdict_v1.json` and `terminal_report_v1.md`
remain interim `inconclusive` evidence snapshots until that finite bisection and
any admitted successor stages finish.
