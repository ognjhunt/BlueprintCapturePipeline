# RoboArena disjoint Reasoner successor (2026-07-28)

This namespace prospectively governs the successor campaign after the terminal
`inconclusive` full-stack calibration. It does not rewrite that history.

The first paid action is limited to one already-exposed, post-unseal diagnostic
pair. Its only purpose is to prove that the pinned Cosmos3-Nano Reasoner runtime
can load and return one exact structured result on a compatible GPU. It cannot
provide ranking, Phase-B admission, WAM qualification, or independent-confirmation
credit.

In parallel, `disjoint_session_candidate_split_v1.json` freezes a label-sealed
candidate set selected from session and policy directory names only. It remains
a same-snapshot candidate until media, action-contract, endpoint, label, and
power admission checks pass. If only replayed actions are technically possible,
the result is open-loop replay qualification and cannot be promoted to live
closed-loop Phase B.

No provider call, GPU allocation, outcome-label read, or data upload is authorized
by these files alone. Paid execution must pass the canonical allocator, exact-source
binding, single-use external authorization, watchdog, budget, teardown, and
provider-zero gates.
