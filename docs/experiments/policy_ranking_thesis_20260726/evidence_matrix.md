# Evidence matrix

| Required claim | Evidence now | What it proves | What remains |
|---|---|---|---|
| Frozen benchmark identity | RoboArena revision `7931db81...`; 3,883 sessions and 10,783 policy episodes | The independent real-policy answer key is attributable and frozen | Pilot/calibration labels were joined only after prediction freeze; held-out labels remain sealed |
| Candidate-policy overlap | 63 released sessions contain the same seven policies; 441 label-blind rows indexed | A complete released cross-policy evaluation matrix is available | OSCAR paper says 65 sessions/455 rollouts; the 14-rollout discrepancy and undocumented subset-selection process create possible selection bias |
| Frozen WAM/evaluator inputs | Protocol v2 `8ed8a7b4...`; evaluator `3c282af9...`; pilot and calibration each have 98 accepted judgments | Splits, gates, model, prompt, schema, frames, sampling defaults, retry rule, cheap baseline, and held-out arm set were fixed before label joins | Held-out attempt 001 blocked after 121/686 and its rows were overwritten; exact attempt 002 produced 0/686 with 686 rate-limit blockers; outcomes remain sealed and no held-out metric exists |
| Held-out resolution | Power artifact `38d2646e...`; 49 session clusters; within-session pairs not treated as independent | Approximately 0.678 pairwise accuracy is needed for 80% power at one-sided alpha 0.05 under the conservative design | Smaller effects, pair-specific ties, or wide registered intervals can only be inconclusive |
| WAM action sensitivity diagnostic | 49/49 pilot action files analyzed without labels or physical-video pixels | Generated pixel motion has positive action-magnitude correlation in 47 nonconstant cases; median Pearson 0.488 | Pixel correlation is not 3-D action following; two cases are undefined and no pass threshold was retrofitted |
| Frozen benchmark calibration | Published OSCAR reference reports Spearman 0.750 and MMRV 0.571; Blueprint pilot/calibration primary pairwise accuracy is 0.6308/0.6726 under the recorded binary-then-partial basis | Both label joins are attributable and the full temporal judge narrowly beats the cheap baseline on pairwise accuracy | Neither partition passes the complete gate set: calibration selective coverage is 0.00885 and action-following pass rate is 0.1020; held-out results do not exist |
| Controlled hybrid scene | NVIDIA Warehouse bundle `65ac09a2...`; sorting-area crate-packing-table USD plus camera-bound 224x224 render; dynamic control report with positive `1d5028eb...` and negative `35be8be2...` | The selected NVIDIA USD workcell is ingested without the full warehouse; the same live MuJoCo interaction seam yields 0.10954 m lift, containment, and stable release for the scripted positive while rejecting the stationary negative | No learned policy ranking exists. Static optional clutter is omitted, Isaac physics was not executed, and the warehouse is neither an independent ranking answer key nor physical evidence |
| Captured-site ingestion | InteriorGS bundle `c19eac1c...`; scene `0787_841244` 3DGS plus separately hashed labels, structure, and local assets | A previously unseen metric 3DGS visual source is retained without a full-site USD rebuild | Dynamic compositing now runs, but collision registration and 3DGS depth occlusion remain unproven; InteriorGS use is restricted to internal non-commercial research with no redistribution |
| Blueprint-captured alternative | Local IMG_3082 capture has passed local QA, a 1.0 scale hint, and NuRec mesh/USDZ outputs | A second Blueprint capture representation exists locally | No explicit rights record or 3DGS artifact was found, so it cannot replace or satisfy the 3DGS transfer gate |
| Hybrid rendering | InteriorGS camera contracts plus dynamic hybrid controls; frozen 300,000-splat 224x224 background; live MuJoCo segmentation for the articulated Panda, can, and tray; live hand-mounted wrist render | Both negative and successful positive controls execute through the exact captured-site observation compositor; the positive reaches 0.10949 m lift, containment, and stable release | The GPU background is explicitly decimated; 3DGS depth occlusion and full captured-site collision geometry remain open |
| Captured-site ranking | Frozen deterministic aggregator; strict three-variant min/max separation; visual contradiction forces abstention | The success predicates, score, uncertainty interval, and abstention behavior were fixed before any learned policy outcome | No prospective learned-policy ordering yet; all 12 learned episodes must execute and produce nonoverlapping intervals or an explicit abstention |
| Four-policy transfer availability | V2 OpenPI PolaRiS cohort totals 47,286,181,297 public bytes; exact generation inventory; per-file size/GCS-MD5 verifier; one-shot four-policy GPU worker; CPU negative preflight | Candidate identities, absolute-joint transforms, checkpoint-byte verification, positive/negative controls, and fail-closed GPU admission are attributable | No learned checkpoint has been downloaded or loaded; checkpoint terms remain implicit; exact-main image release and one paid GPU execution remain |
| GPU launch readiness | Private 91,229-byte input bundle with no raw 3DGS; live zero-inventory probe; advisory A40 48 GiB at $0.44/hour; one-GPU 100-GiB redacted request dry-run; completed AMD64 build graph; canonical allocator dispatch with budget reservation, independent watchdog, and pending-teardown gates | Current provider credentials/capacity, request shape, rights boundary, secret redaction, target image build graph, and mutation ordering pass without provider mutation | Digest publication from exact merged main, fresh preflight, execution, output retrieval, and provider-zero teardown evidence remain |
| Speed/cost advantage | Local stages used no paid cloud GPU; the full 630,898-splat four-view render took 41.4 seconds and the square-camera candidate/final rerenders took 17.95/8.77 seconds; pilot/calibration judging recorded $3.0161225/420.179 seconds and $2.89694/474.981 seconds | Captured-site visual ingestion and camera revision are cheap and measured; physical action execution lower bounds and judge measurements are attributable | WAM/policy GPU time, full end-to-end benchmark time, and attributable physical monetary cost remain unmeasured |

No row upgrades a generated-video score, simulator result, or prospective captured-site ranking into physical success.
