# Evidence matrix

| Required claim | Evidence now | What it proves | What remains |
|---|---|---|---|
| Frozen benchmark identity | RoboArena revision `7931db81...`; 3,883 sessions and 10,783 policy episodes | The independent real-policy answer key is attributable and frozen | Pilot/calibration labels were joined only after prediction freeze; held-out labels remain sealed |
| Candidate-policy overlap | 63 released sessions contain the same seven policies; 441 label-blind rows indexed | A complete released cross-policy evaluation matrix is available | OSCAR paper says 65 sessions/455 rollouts; the 14-rollout discrepancy and undocumented subset-selection process create possible selection bias |
| Frozen WAM/evaluator inputs | Protocol v2 `8ed8a7b4...`; evaluator `3c282af9...`; pilot and calibration each have 98 accepted judgments | Splits, gates, model, prompt, schema, frames, sampling defaults, retry rule, cheap baseline, and held-out arm set were fixed before label joins | Held-out attempt 001 blocked after 121/686 and its rows were overwritten; exact attempt 002 produced 0/686 with 686 rate-limit blockers; outcomes remain sealed and no held-out metric exists |
| Held-out resolution | Power artifact `38d2646e...`; 49 session clusters; within-session pairs not treated as independent | Approximately 0.678 pairwise accuracy is needed for 80% power at one-sided alpha 0.05 under the conservative design | Smaller effects, pair-specific ties, or wide registered intervals can only be inconclusive |
| WAM action sensitivity diagnostic | 49/49 pilot action files analyzed without labels or physical-video pixels | Generated pixel motion has positive action-magnitude correlation in 47 nonconstant cases; median Pearson 0.488 | Pixel correlation is not 3-D action following; two cases are undefined and no pass threshold was retrofitted |
| Frozen benchmark calibration | Published OSCAR reference reports Spearman 0.750 and MMRV 0.571; Blueprint pilot/calibration primary pairwise accuracy is 0.6308/0.6726 under the recorded binary-then-partial basis | Both label joins are attributable and the full temporal judge narrowly beats the cheap baseline on pairwise accuracy | Neither partition passes the complete gate set: calibration selective coverage is 0.00885 and action-following pass rate is 0.1020; held-out results do not exist |
| Cross-lane pipeline identity | Lane A used released OSCAR WAM rollouts plus evaluator `3c282af9...`; Lanes B/C used OpenPI-in-MuJoCo plus the same deterministic state scorer and aggregator | The policy/simulator/scorer contracts were stable from Warehouse to InteriorGS, and protocol v2 explicitly admitted deterministic predicates for Warehouse | OSCAR was not executed in transfer, the Lane A evaluator digest and seven-policy cohort were not reused in Lane C, and the registered frozen-stack transfer condition is unmeasured |
| Controlled hybrid scene | NVIDIA Warehouse bundle `65ac09a2...`; sorting-area crate-packing-table USD plus camera-bound 224x224 render; dynamic controls; 12 exact-main learned-policy episodes | The selected workcell is ingested without the full warehouse; one frozen deterministic evaluator handled every policy. All episodes were contract-valid, total ranking abstained, and only pi0.5 > pi0-FAST had strict interval separation | Zero episodes achieved transport or containment. Static optional clutter is omitted, Isaac physics was not executed, and the warehouse is neither an independent ranking answer key nor physical evidence |
| Captured-site ingestion | InteriorGS bundle `c19eac1c...`; scene `0787_841244` 3DGS plus separately hashed labels, structure, and local assets | A previously unseen metric 3DGS visual source is retained without a full-site USD rebuild | Dynamic compositing now runs, but collision registration and 3DGS depth occlusion remain unproven; InteriorGS use is restricted to internal non-commercial research with no redistribution |
| Blueprint-captured alternative | Local IMG_3082 capture has passed local QA, a 1.0 scale hint, and NuRec mesh/USDZ outputs | A second Blueprint capture representation exists locally | No explicit rights record or 3DGS artifact was found, so it cannot replace or satisfy the 3DGS transfer gate |
| Hybrid rendering | InteriorGS camera contracts plus dynamic hybrid controls; frozen 300,000-splat 224x224 background; live MuJoCo segmentation for the articulated Panda, can, and tray; full-rotation mocap-mounted wrist camera | Both negative and successful positive controls execute through the captured-site compositor; the positive reaches 0.10949 m lift, containment, and stable release. Before learned execution, native MuJoCo camera-matrix validation passed at `2.64e-16` maximum absolute error | Earlier saved control wrist frames used a free camera that discarded optical-axis roll and are superseded; their deterministic object-state outcomes remain valid. The GPU background is explicitly decimated; 3DGS depth occlusion and full captured-site collision geometry remain open |
| Captured-site ranking | Frozen deterministic aggregator; strict three-variant min/max separation; 12 exact-main InteriorGS episodes | The same Lane B/C sim pipeline executed all four policies without per-policy scoring changes. The frozen rule abstained from a total ranking and emitted only pi0-FAST > pi0-100k as a separated pair | Zero episodes achieved transport or containment. Three-variant range-separation power and abstention correctness are unvalidated; this is prospective, not physical, and does not satisfy the useful four-policy-ordering claim |
| Four-policy transfer execution | V2 cohort 47,286,181,297 verified bytes; 24 GPU episodes; output archive `2dd36776...`; validation `b751b80f...` | All four exact checkpoints loaded and executed once per policy over both scene lanes and three variants; 24/24 contracts were valid | The runtime dynamically fetched the FAST tokenizer helper without an explicit logged revision, so complete provenance is limited |
| GPU execution and teardown | Exact-main digest `f8f4dc01...`; private input `52140529...`; Vast instance `45955527`; recovery evidence `f76955b1...` | RTX 6000 Ada/JAX execution, immutable image/input identity, signed output, exact/prefix/global absence, closed teardown, released lease, and settled budget are proven | A Vast post-delete HTTP-200/`instances: null` classification bug required a tested control-plane recovery; it did not change episode results |
| Speed/cost advantage | Builder 1,319.3 s/$0.061081; learned-policy GPU 991 s/$0.206458; combined 2,310.3 s/$0.267539; prior benchmark judge costs/times remain recorded | The current cloud execution is inexpensive and fully measured | Substantial speed advantage and lower physical cost are not proven: physical total monetary cost is unmeasured and the attributed benchmark offline path was slower than the physical action-only lower bound |

No row upgrades a generated-video score, simulator result, or prospective captured-site ranking into physical success.
