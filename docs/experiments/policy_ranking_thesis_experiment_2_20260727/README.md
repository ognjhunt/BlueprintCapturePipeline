# Policy-ranking thesis Experiment 2 evidence

Terminal verdict: `thesis_not_supported`.

Read `final_report.md` for the narrative and `final_verdict.json` for the machine-readable decision. The two required components remain separate:

- `frozen_benchmark_calibration_report.json`: `not_supported`
- `interiorgs_captured_transfer_report.json`: `not_supported`

The held-out GPT-5 ranking matrix was closed incomplete at 43/686 under the user-authorized `$10` combined model-API cap. Labels remained sealed and partial predictions were not scored. The negative benchmark component comes from the sufficiently powered, label-free held-out causal-conditioning failure, not from an invented partial ranking result.

Key evidence:

- `heldout_causal_conditioning_report.json`: powered 49-cluster causal diagnostic
- `causal_power_reestimate.json`: development-only power reestimate
- `provider_evidence/heldout/`: immutable hash-chained provider journal and manifest
- `provider_matrix_closure.json`: exact partial-execution and cost closure
- `risk_coverage_curves.json` and `policy_rankings_with_confidence_intervals.json`: explicit unavailability records
- `economics_and_time_report.json`: attributable spend/time and physical-counterfactual limits
- `validation_report.json`: focused/full tests, lint, lock, integrity, and secret-scan evidence
- `statistical_correction_amendment_002.json`: session-clustered validity-interval correction found during review; gates and verdict remain unchanged
- `label_access_incident_001.json`: procedural deviations
- `successor_model_decision.json`: user-selected GPT-5 mini successor and why it was not inserted into the frozen holdout
- `successor_wam_decision.json`: general Cosmos3-Nano forward-dynamics qualification design, hardware choice, and cross-embodiment/warehouse boundaries
- `cheapest_next_experiment.json`: current public-snapshot check and the lowest-cost valid successor design
- `evidence_claim_matrix.md`: claim-by-claim proof boundaries for every artifact group
- `limitations.md` and `unresolved_contradictions.md`: proof boundaries

This is a scoped falsification of the frozen OSCAR/evaluator/captured-transfer system, not a universal claim about all future WAMs, models, robot tasks, or newly collected benchmarks.
