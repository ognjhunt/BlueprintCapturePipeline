# Website development-surface camera scope

ADP-009D / day-21. The website-origin native attempt on `1ef00878` rendered the
owner-authorized plain development surface. External and overview frames showed
the object but failed the captured-site dominant-background-color check. The
worker unconditionally requested captured-site appearance even though the sealed
support record said `authored_development_surface`.

Packet materialization now marks only explicit authored development geometry
with a camera scope and `captured_scene_evaluation_allowed=false`. The support
must disclaim measured scale, the definition must disclaim physical truth, and
the appearance representation must be USD geometry. Default, captured, NuRec,
and splat paths retain the captured-site requirement.

Construction derives its camera expectation from that sealed plan. Controls and
policy admission use the same expectation. Development frames still need actual
rendered radiance, a visible target in the initial external view, and the
existing image/framing checks. They never claim captured-site appearance. This
does not relax robot reach, orientation, collision, grasp or task success gates.

Verification: 165 tests passed across packet/camera/construction, controls and
diagnostic policy admission, policy worker, the native policy lifecycle rehearsal,
and provider runtime import closure. Blank and target-absent frames still fail;
captured-site admission rejects the same plain-surface frames.

An offline replay copied the original request and hash-verified assets from
`team-eval-b483310b2a66e7e417d7b88da6ef12dc-franka-controls-1ef008781d5e-episode-construction-launch`.
With network forbidden, it rematerialized the packet. Only `camera_scene_scope`,
`claim_boundary`, and `plan_digest` changed; robot, task and assets were identical.
New plan: `sha256:a2f6461d11d3ee0ca8255d3a8ab01a7c0e077892fb90f827f295bbde742957ac`.
Replay: `/private/tmp/blueprint-development-camera-replay/replay.py`.
No GPU was rented, no captured-scene evaluation was qualified, and live native
placement/grasp recovery remains to be demonstrated.
