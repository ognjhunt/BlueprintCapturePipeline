# Local integration handoff

Implementation commit: `37eabbf04f9c9e789a4cfc5b470ce93a2bb1b421`.
Branch: `codex/policy-comparison-audit-20260907-e524`.
Worktree: `/private/tmp/bcp-policy-comparison-audit-20260907-e524`.
Base: `c217cf39659d5c8c4988dd463d02b7450b5c6345`.
Final observed remote main: `da3ff3ba25e411dffc64358655598ba7e5470b15` (#1764). No overlap in the six changed production files at that refresh.

The implementation is committed locally and unpushed. This audit owns no live runtime, deployment, provider, model, notification, queue or lock. The execution owner retains those responsibilities. Do not treat this handoff as launch or deployment authorization.

Review `AUDIT.md` for seven reproduced findings, the complete parity/invariant matrix and remaining runtime boundaries. The patch changes existing session/worker/episode/scorer/media/rescore seams. It introduces no new policies, evaluation campaign, thresholds, schemas, runtime selection, published profiles or physical claims.

Reviewable behavior changes:

1. Session readers refuse within-candidate checkpoint/runtime drift and contradictory resolved scenarios.
2. Child aggregation verifies child seals and exact frozen cell parameters before aggregation; order is normalized while artifact locations remain bound to selected cell index.
3. Readiness retains task reset readbacks and checks restoration before the first policy query, including the final reset after readiness. This still does not prove all camera/physics reset fields.
4. Sparse native rigid trajectories refuse scoring. An existing synthetic collision fixture now supplies the intervening samples while preserving its step-72 event.
5. Media readers reject JPEG substitutes, inconsistent dimensions/counts/timestamps/kinds and misclassified terminal observations.
6. Scorer identity includes the split rigid and retreat implementations. Old receipt bytes remain immutable; any correction is a new overlay.
7. Completed new nested episodes with unscored/undetermined outcomes remain completed diagnostics but are not marked interpretable.

Verification at the implementation commit: **43 new regressions passed** under OS network denial. Before committing the identical production tree, 62 session/lifecycle tests passed, including both mandatory lifecycle/import suites; the focused boundary run passed 439 tests before the final interpretation change, and its affected session surface was rerun. Client/regression verification passed 90 tests under OS network denial. Counts overlap. `evidence/tested-revisions.json` records the revision relationship; logs are retained.

Commands (from this worktree; all use the pinned interpreter, explicit file lists, low concurrency and `sandbox-exec` network denial):

```bash
bash audit/policy-comparison-20260907/VERIFY.sh regressions
bash audit/policy-comparison-20260907/VERIFY.sh lifecycle
bash audit/policy-comparison-20260907/VERIFY.sh clients
bash audit/policy-comparison-20260907/VERIFY.sh focused
```

- `regressions`: the actual reproduced identity/reset/media/scorer/aggregation refusals and analytic controls.
- `lifecycle`: the final session surface plus required real-worker lifecycle and provider import closure; no actual Isaac/GPU/model.
- `clients`: real Python client wrappers and GR00T wire codec against fake transports; intentionally excludes the real loopback-server test.
- `focused`: the named adjacent contracts and existing boundary tests, not a repository-wide test lane.

The fixed implementation can be reviewed/cherry-picked by the execution owner when appropriate. Refresh remote main before integration, reconcile any new overlap, and repeat only checks affected by subsequent edits. Do not backfill or edit a historical result in place. The new sparse-sample rule may refuse old trajectories that previously produced an outcome; retain that refusal and original receipts. The new reset fields extend a digest-bound readiness receipt without retroactively requiring them of old receipt readers.

Website owner packet: `evidence/consumer-handoff.json`, generated by `consumer_probe.py`. It reproduces actual producer marginals of 2/6 versus 4/6 while the two mutually scorable pairs both favor the first candidate. Current Website main remains `5b07fe48e93cabf2a758b7740aab3d6594cbdd10`. This session changed no TypeScript and sent no message. The paired display must describe the same cohort as its significance counts, with marginals separately labeled.

Historical Scene 839873 terminal execution/billing/provider-zero remains represented by the supplied prior audit. No historical media archive or live host was reopened. Runtime camera/physics/contact reset parity, sensor freshness and server-side model behavior remain unverified by this offline deliverable; physical trials are neither claimed nor pursued.
