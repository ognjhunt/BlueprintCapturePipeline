`dist/`, `package.json`, `README.md`, and `LICENSE` are the unmodified
`@meta-sam/parser` 0.0.12 npm package from Meta. The published package integrity
is `sha512-ccYumIRRSXs4Txyjgsl4k6SwHXd6Yj6ARzXA8jaF4kCN72YxiOG06s58p1MBLMicKMRkJCKIi7apcE5EIQI9fw==`.

`runner.mjs` is Blueprint code that calls the parser. Keep Meta's `LICENSE` with
the package on every redistribution. The parser is CPU output decoding, not a
second segmentation model.
