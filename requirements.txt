# Blueprint Capture Pipeline - Core Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.24.0
Pillow>=10.0.0
pyyaml>=6.0
opencv-python-headless>=4.8.0  # Use headless for server environments
scipy>=1.11.0

# PyTorch (CPU version - use pytorch.org for GPU)
torch>=2.0.0
torchvision>=0.15.0

# 3D Reconstruction
plyfile>=1.0.0
tqdm>=4.65.0
pycolmap>=3.10.0  # Python bindings for COLMAP - no CLI needed

# Difix3D+ Scene Inpainting (for HQ splats and gap filling)
# These enable the Difix3D+ refinement stage in the pipeline
diffusers>=0.25.0  # Hugging Face Diffusers for Difix model
transformers>=4.36.0  # Required by diffusers
accelerate>=0.25.0  # GPU acceleration for diffusion models
lpips>=0.1.4  # Perceptual loss (optional but recommended)

# Cloud dependencies (optional - install separately for GCS support)
# google-cloud-storage>=2.10.0
# google-cloud-run>=0.10.0
# google-cloud-logging>=3.6.0

# For CUDA-accelerated 3DGS (10-100x faster, optional):
# pip install git+https://github.com/graphdeco-inria/diff-gaussian-rasterization.git
# pip install git+https://github.com/camenduru/simple-knn.git

# For Difix3D+ model (automatic download on first use):
# Model: nvidia/difix or nvidia/difix_ref
# See: https://github.com/nv-tlabs/Difix3D
