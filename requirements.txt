# Canonical dependency metadata lives in pyproject.toml.
# This compatibility file keeps older `pip install -r requirements.txt` flows
# on the CPU-safe default package install.
# cache-bust: 2026-03-16
.
