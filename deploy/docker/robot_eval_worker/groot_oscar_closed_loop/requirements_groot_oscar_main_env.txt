# OSCAR-client deps installed into /opt/oscar-venv on the pinned Isaac Sim 6
# carrier. Torch, cuDNN, OSCAR source requirements, and the TE shim are installed
# separately so they cannot replace Isaac's Python environment.
#   pip install -e . mujoco pillow pyzmq msgpack msgpack-numpy imageio uv huggingface_hub
# Intentionally NO torch/torchvision here so the base's cu128 build is untouched.
mujoco
onnxruntime==1.20.1
pillow
pyzmq
msgpack
msgpack-numpy
imageio
uv
huggingface_hub
